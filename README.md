# Nexa Builder — STONIC Ready

This build carries the full roadmap discussed:
- one-time Gemini setup with automatic model discovery
- prompt -> real professional website generation
- sequential build stages tied to real generation work
- two-pass generation and quality improvement
- finished website preview after completion
- separate full-screen Preview
- Edit with Nexa AI flow
- export generated website
- architecture ready for future agent, automation and STONIC voice integration
- publishing kept separate so a secure GitHub/server publishing layer can be added later

Important: this personal GitHub Pages build calls Gemini from the browser. Do not expose a personal API key in a public multi-user SaaS. For a public product, move AI calls to a server-side function and store secrets in server environment variables.
